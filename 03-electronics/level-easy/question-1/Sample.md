.  Create a program using Arduino IDE that simulates a standard traffic light sequence.  

.  Display the traffic light statuses directly as text in the Serial Monitor instead of relying on physical LEDs

. Set a Specific Time Interval for eacxh signal so that it imitates real life traffic signals
